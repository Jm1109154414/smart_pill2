# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# Service Role (Only for Edge Functions - DO NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# VAPID Keys for Web Push Notifications
# Generate with: npx web-push generate-vapid-keys

# Client-side (exposed to browser - Frontend/Public variables in Lovable)
VITE_VAPID_PUBLIC=your-vapid-public-key

# Edge Functions Secrets (Set in Lovable → Settings → Environment → Server/Secrets)
# VAPID_PUBLIC=your-vapid-public-key
# VAPID_PRIVATE=your-vapid-private-key

# Storage
REPORTS_BUCKET=reports
